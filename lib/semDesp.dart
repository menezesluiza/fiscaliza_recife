import 'package:flutter/material.dart';

class SemDesp extends StatelessWidget {
  const SemDesp();

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 70,
      alignment: Alignment.center,
      child: Text('NENHUMA DESPESA NO PERÍODO'),
    );
  }
}
