import 'package:flutter/material.dart';

class SemRecArrec extends StatelessWidget {
  const SemRecArrec();

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 70,
      alignment: Alignment.center,
      child: Text('SEM RECEITA ARRECADADA'),
    );
  }
}
